# Committed reference of env keys. Real values live in .env (gitignored).
# Per assignment spec: ".env file [for] every sensitive key values in .env and its key in .env.local"
MONGODB_URI=
REDIS_URL=
JWT_SECRET=
ANTHROPIC_API_KEY=
PORT=
CORS_ORIGIN=
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=
